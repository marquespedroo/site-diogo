/**
 * Authentication Module - ImobiTools
 * Exports auth context, hooks, and utilities
 */

export { AuthProvider, useAuth, useRequireAuth, useRequireStaff } from './AuthContext';
export type { UserProfile, AuthState, AuthContextType } from './AuthContext';
